{{!-- <div class="vh-100 mt-5" style="background: #6a11cb">
    <div class="d-flex flex-wrap justify-content-center ">
        {{#each games}}
        <div class="card my-5 mx-5" style="width: 300px;">
            <img src="{{img}}" class="card-img-top" alt="...">
            <div class="card-body text-center">
                <h4 class="card-title">{{title}}</h4>
                <h6 class="card-text">Precio: {{price}}$</h6>
                <p class="card-text">Articulo vendido por:<br>{{addedBy.username}}.</p>
                <a href="/user/fav/{{_id}}" class="btn btn-primary">Añadir a favoritos</a>
            </div>
        </div>
        {{/each}}
    </div>
</div> --}}